apiVersion: v1
kind: Secret
metadata:
  name: agent-relay-postgres
type: Opaque
# LOCAL/DEMO ONLY: the versioned password is an intentionally non-sensitive
# placeholder for this kind homework. Never reuse this Secret for a real deployment.
stringData:
  POSTGRES_DB: agent_relay
  POSTGRES_USER: agent_relay
  POSTGRES_PASSWORD: local-demo-placeholder-not-for-production
